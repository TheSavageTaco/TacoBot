const Discord = require('discord.js');
module.exports = (client, message, args, member) => {

  /*let chan = client.channels.find('id', '391005495743217664');
  const embed = new Discord.RichEmbed()
  .setColor(0x00AE86)
  .setTimestamp()
  .addField('Wecome:',
    ${user.tag} `to F.U.N.! :white_check_mark:`)
  return message.defualtChannel.send({embed});*/
};
