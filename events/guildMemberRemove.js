module.exports = member => {
  let guild = member.guild;
  //guild.channels.get('365671468513624078').send(`${member.user.username} left Paradise.`);
};


//change channel id, defaultChannel makes bot laggy/crash cuckle
